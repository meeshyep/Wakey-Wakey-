import 'react-native';
import React from 'react';
import renderer from 'react-test-renderer';
import moment from "moment";
import TimePicker from "../app/TimePicker"
import {AlarmSetButton} from "../app/TimePicker"

describe('Time picker', ()=>{
  it('updates the state with the selected time', ()=>{

  });

});
describe('AlarmSetButton', ()=>{
  it('confirms the selected time', ()=>{
console.log((new TimePicker(TimePicker.defaultProps)));

  });

});
